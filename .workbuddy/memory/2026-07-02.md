# 2026-07-02

- Created UI redesign package for SingBox proxy client based on user-provided screenshot.
- Wrote `design-system.md` with complete tokens, components, responsive rules, and accessibility specs.
- Built interactive five-screen prototype in `prototype.html` (Overview / Nodes / Logs / Settings / About).
- Wrote `overview.md` summarizing scope, decisions, and implementation follow-up steps.
- Did not overwrite `index.html` because it is the Vite entry point; prototype is standalone.
- Removed redundant connection-status block from workspace header; status is now only in sidebar Session card. Header simplified to toggle + route/port + mode pills.
- Added Saved Profiles section to sidebar with 4 profile entries (local, URL subscriptions), click-to-switch active state.
- Cross-referenced another conversation's cleaner SingBox UI design. Identified 6 improvement points; implemented 3:
  1. Migrated theme toggle from `.light` class to `[data-theme="light|dark"]` attribute (semantic, syncs with `prefers-color-scheme`).
  2. Added hero connection card to Overview page — 72px status icon with glow, large "Protected/Offline" text, big toggle, uptime/transfer stats.
  3. Added 4 preference summary cards above Settings tabs (Autostart, Inbound, TUN, DNS) showing current runtime state at a glance.
- Implemented Option C layout: removed workspace header control-bar entirely, moved global connect/disconnect toggle into sidebar Session card as the single source of truth. Sidebar switch syncs bidirectionally with Overview hero switch. Updated JS to handle session dot, pill, and helper text state changes.
- Redesigned Logs page to match the real terminal look:
  - Removed old filter chips and search; replaced with a stats toolbar (ENTRIES / STREAM Live / ERRORS / WARNINGS).
  - Added Pause, Copy, Auto-scroll toggle, and Clear action buttons.
  - Made log stream nearly full-screen with `flex: 1` and `overflow: auto`.
  - Widened timestamp column to fit full `+0800 YYYY-MM-DD HH:MM:SS` format.
  - Added realistic sample log rows including ANSI-style markers (`[36mINFO[0m`) and process paths.
  - Implemented live log simulation: appends a new entry every 1.8s and auto-scrolls to bottom when Auto-scroll is on and not paused.
- Added profile hover actions: each profile row now reveals edit (pencil) and export (download) buttons on hover, with tooltips. The meta tag fades out to make room.
- Profile "Edit rules" button navigates to Settings → Rule Sets tab, ready for rule editing.
- Redesigned sidebar Saved Profiles section as an upward-expandable profile selector:
  - Replaced the scrollable `profile-list` with a compact `profile-trigger` card that shows the active profile and a chevron.
  - Clicking the trigger expands a dropdown menu upward with all available profiles, full names, type tags, and hover-revealed edit/export actions.
  - Removed native scrollbars and text truncation issues from the profile list.
  - Added `data-type` attributes to each profile for reliable type-tag synchronization when switching.
  - Dropdown closes on outside click or when selecting a profile; edit-rules buttons still navigate to Settings → Rule Sets.
- Fixed large-screen empty space on Overview and Nodes pages:
  - Overview: snapshot tiles now use horizontal layout (icon left, text right) so they look good when stretched.
  - Overview: bottom card grid changed from 2 columns to responsive 3 columns (>1350px) / 2 columns (≤1350px) / 1 column (≤1100px); added a third "Network Stats" card.
  - Nodes: replaced single centered group card with a 2-column `nodes-layout` — group list (left, ~60%) + sticky selected-node detail panel (right, ~40%) on wide screens.
  - Nodes: detail panel shows flag, name, latency bar, protocol/transport/server/port, and status; updates when clicking a node row.
  - Added responsive breakpoints so the detail panel stacks below the group list on screens ≤1350px and mobile.
- Fixed Nodes page not fully using available workspace width:
  - Added `.workspace { flex: 1; min-width: 0; display: flex; flex-direction: column; }` so the main content area expands to fill the remaining width after the sidebar.
- Removed toggle switch from sidebar Session card; entire `.session-card` is now clickable:
  - Deleted `.session-toggle-row` HTML + CSS (label + checkbox switch).
  - Added `cursor: pointer`, hover `border-color` highlight, and `:active` scale effect to `.session-card`.
  - Replaced `#sessionSwitch` DOM checkbox with a `let connected` JS state variable.
  - Card click toggles `connected`, syncs hero switch, and updates all connection UI.
  - Hero switch change handler now updates `connected` directly (no cross-element checkbox sync needed).

- Fixed ConfigOverviewPanel: removed Status info card (right column), changed grid from 3-col to 2-col.
  - Optimized section tabs (Nodes/DNS/Rules/Rule Sets): larger 16px icons, capsule-style buttons with `rounded-full`, active state uses `bg-primary-500/10 border-primary-500/30 text-primary-500 shadow-sm`, hover state uses `border-primary-500/25`.
  - When non-nodes tab is active, right column shows a muted "Switch to Nodes" placeholder card.
- Fixed NodeList white-screen crash: `settings.transport` and `settings.host` in sing-box config are often objects (e.g. `{type: "ws", path: "/ws"}`). React crashes when rendering objects as children. Fix: wrap all settings-derived values with `String()` before passing to `<DetailKv>`.
  - `const transport = settings.transport != null ? String(settings.transport) : undefined;`
  - `const host = hostRaw != null ? String(hostRaw) : undefined;`
  - Commit 3806e3c.
- Simplified Overview section tabs: removed count badges from each tab; counts already visible in content-card headers. Unified segmented-control style with subtle border and active elevation.
  - Commit 5f3ebc7.
- Added loading states for core toggle and node switching, removed "Switch to Nodes" placeholder:
  - `Sidebar`: accepts `loading` prop; Session card shows spinner + "Processing" pill when starting/stopping, ignores clicks during transition.
  - `ConfigOverviewPanel`: accepts `loading` prop; hero toggle thumb shows animated loader and is disabled while core transitions.
  - `NodeList`: nodes/groups matching `selectedOutboundTag` during `runtimePhase === "switching"` show "Switching" badge with spinner.
  - Removed "Switch to Nodes" placeholder from DNS/Rules/Rule Sets tabs; Overview grid becomes single-column for non-node tabs.
  - Added missing `.status-chip` / `.status-chip-primary` utility classes in `global.css`.
  - Commits d4cc88f, 20e9800.

### Phase 3 — src page components visual alignment (commit b5ae5e8)
**ConfigOverviewPanel** (rewritten):
  - Added hero connection card at top: centered, 72px shield icon with green glow when connected, status label, toggle switch, 3-column stats row (Route / Uptime / Transfer).
  - Required new props: `isRunning`, `onToggleProxy` (passed from App.tsx).
  - Redesigned snapshot tiles to horizontal layout: 38px icon in rounded-xl bg-primary-500/12, label above, value + meta below.
  - 3-column overview grid: left column = section content card (Groups/DNS/Rules/RuleSets depending on tab), middle = Proxy Nodes card (only for nodes tab), right = Status info card with key-value pairs.
  - OverviewCard sub-component renders sorted item list with colored badges and optional edit buttons.
  - Removed unused imports (Layers3, Sparkles left over from old version).

**NodeList** (rewritten):
  - Left/right split layout: `grid-cols-[minmax(0,1fr)_minmax(340px,480px)]` — node list fills left, detail panel on right.
  - NodeDetailPanel: sticky at top, shows flag emoji (guessed from node name via FLAG_MAP), protocol badge, latency with gradient bar, key-value grid (protocol, server, port, transport, host, ID, status).
  - Clicking any node row (`handleSelectNodeRow`) populates and shows the detail panel.
  - Empty state: dashed border placeholder with "Select a node to view details" message when no node selected.
  - Fixed `ProxyNode` type — no `tag` property exists, used `id` + `name` instead.
  - Removed unused `selectedGroup` and `selectedNode` local vars (were unused after detail panel refactor).

**SettingsPanel** (polished):
  - Insight cards changed to horizontal SnapshotTile style (icon left, text right, same as Overview snapshot tiles).
  - Tab navigation: larger buttons with 8×8 icon block, `bg-primary-600/15` active state.
  - ToggleRow: added border + hover lift effect (`hover:-translate-y-[1px]`), consistent with design system.
  - Save/Reload buttons now have Check + Settings2 icons.
  - SectionHeader font size bumped to text-base.

**All phases summary**:
  - Theme: `data-theme` attribute, design tokens from prototype, legacy `--color-*` aliases
  - Shell: Sidebar without toggle switch (click card), TitleBar simplified, App layout flex-col
  - Pages: ConfigOverviewPanel (hero + snapshots + grid), NodeList (split + detail), LogViewer (full terminal), SettingsPanel (insight cards), AboutPanel (brand header)
  - TypeScript clean, production build: 255 KB JS / 40 KB CSS
  - 3 commits: prototype (87e0f3c), phase 1-2 shell (ff6b38c), phase 3 pages (b5ae5e8)
- Commit 7c8e0a1: prototype + design-system + overview committed.

### src migration — Phase 1+2 (commit ff6b38c)

**Theme system**: Migrated from `darkMode: "class"` to `[data-theme="dark"]` selector. New design tokens: `--bg-*`, `--primary-*`, `--text-*`, `--border*` with `--color-*` legacy aliases. Updated `useTheme.ts`, `tailwind.config.js`, `global.css`.

**Shell layout**: Removed `ProxyControl` (error bar + top control bar). Sidebar rewritten: session card is click-to-toggle, profile selector is upward-expandable dropdown, workspace nav uses prototype active styles. TitleBar simplified. App restructured to `flex-col` layout.

**Pages**: `LogViewer` → full-screen terminal (Cascadia Code, live stats, level filters). `AboutPanel` → centered brand header + diagnostics card.

**Remaining pages** (ConfigOverviewPanel, NodeList, SettingsPanel) still use old styling and need Phase 3 visual updates to match prototype.

